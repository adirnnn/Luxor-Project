version: '3.8'

services:
  frontend:
    build: .
    container_name: luxor-compose
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development